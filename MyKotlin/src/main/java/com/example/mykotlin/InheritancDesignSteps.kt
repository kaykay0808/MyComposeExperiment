package com.example.mykotlin

// 1. Look at object that have common attributes and behaviours (Fields, properties and functions).

// 2. Design a class that represent the common state and behaviour. ()

// 3. Decide if a subclass needs behaviours (method implementations) that are specific to that particular subClass type.

